
const sampleText = "hElLo, WORld"
const lower=sampleText.toLowerCase()
firstInitial=sampleText.slice(0,1)
restOfText=sampleText.slice(1,)
console.log(firstInitial.toUpperCase() + restOfText.toLowerCase())


